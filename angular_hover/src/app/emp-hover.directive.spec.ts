import { EmpHoverDirective } from './emp-hover.directive';

describe('EmpHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new EmpHoverDirective();
    expect(directive).toBeTruthy();
  });
});
